"""Biological-inspiration prototypes (RFC0070, RFC0072, RFC0073).

These are standalone, dependency-free sketches of the hybrid-memory vision —
they are NOT wired into the core pipeline (unlike the epigenetic module, which
graduated into `core/adaptation.py`). They live here so the repo root stays
focused on the runtime memory system, and so the README stops implying the core
already calls them.

See FUTURE.md §3.3 for the per-module "wire it in vs keep as prototype" decision.
"""
