name: Crystal Pre-Freeze Wave 2

on:
  pull_request:
    branches:
      - main
    paths:
      - tools/pre_freeze_wave2_patch.py
      - .github/workflows/pre-freeze-wave2.yml

permissions:
  contents: write

jobs:
  apply-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install -e .
      - name: Apply Wave 2 patch
        run: python tools/pre_freeze_wave2_patch.py
      - name: Compile Wave 2 surfaces
        run: python -m compileall -q core tests/test_trace.py tests/test_pre_freeze_wave2.py
      - name: Run targeted Wave 2 regressions
        run: |
          pytest -q -o addopts='' \
            tests/test_trace.py \
            tests/test_pre_freeze_wave1.py \
            tests/test_pre_freeze_wave2.py
      - name: Commit validated Wave 2 changes
        run: |
          git config user.name "velantrim-pre-freeze-bot"
          git config user.email "actions@users.noreply.github.com"
          git add \
            core/pipeline.py \
            core/query_pipeline.py \
            core/trace.py \
            tests/test_trace.py \
            tests/test_pre_freeze_wave2.py
          if git diff --cached --quiet; then
            echo "No generated changes to commit"
            exit 0
          fi
          git commit -m "fix(pre-freeze): pin retrieval semantics and add TRACE v2"
          git push origin HEAD:agent/crystal-pre-freeze-remediation-2026-08-18
