# Branding PR Summary

This branch establishes a clear naming hierarchy without changing runtime or compatibility identifiers:

```text
Velantrim Verifiable Memory = current product
Crystal = current kernel codename
Velantrim ExoCortex = long-term research vision
```

The GitHub repository slug and project title are intentionally left for the repository owner to change manually after review.