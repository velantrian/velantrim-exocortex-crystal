# Manual Owner Step

After this documentation branch is reviewed, the repository owner may manually rename the GitHub repository to `velantrim-verifiable-memory` and update the repository title, description and topics. No automated repository rename is performed by this branch.