# Issue #317 validation marker

This file exists only to trigger the permanent CI workflow on the `claude/**` validation branch for the draft implementation in PR #321.

It does not change runtime behavior, truth boundaries, grant scope or the verified checkpoint.
