# Branding Review Boundary

This branch is documentation-only.

Reviewers should confirm:

- the public product name explains what the repository does;
- Crystal remains clearly identified as the kernel codename;
- ExoCortex remains clearly marked as research vision;
- no research capability is represented as implemented runtime;
- no package, CLI, URL, import, schema or runtime identifier is renamed;
- the manual rename checklist is complete enough for the repository owner;
- existing implementation and test claims are not changed by this branch.

No runtime tests are required solely because these files are added, but Markdown link and terminology review is required.