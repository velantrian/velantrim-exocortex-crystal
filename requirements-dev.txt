# Velantrim ExoCortex — development and test requirements
#
# The runtime package intentionally has no mandatory third-party dependencies.
# Use this file only for local development, tests and CI.
#
# This file MUST stay aligned with the `[dev]` extra in pyproject.toml:
# `pip install -r requirements-dev.txt` and `pip install -e '.[dev]'` are the
# same documented path (CI uses this file; contributor docs use the extra).

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# For exercising the optional FastAPI service layer (core/api.py) in CI.
# Not part of the runtime: the default install stays standard-library only.
fastapi>=0.110.0
httpx>=0.27.0

# Optional layers exercised by the test suite so coverage reflects the whole
# codebase (NOT runtime deps — the default install stays standard-library only):
#   cryptography → core/crypto.py Fernet backend (GDPR Art. 32)
#   pyyaml/pypdf/rdflib → core/adapters/* knowledge adapters (WP4)
cryptography>=42.0.0
pyyaml>=6.0
pypdf>=4.0
rdflib>=7.0
