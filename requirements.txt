# Velantrim ExoCortex — runtime dependencies
# MVP uses only stdlib, but pytest required for tests.

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Optional L3 graph backend (embedded, Cypher; successor to the frozen Kuzu).
# Default backend is 'mock' (stdlib only); install to enable LadybugDB:
#   pip install ladybug   (pulls numpy)
# Persistent mode: VELANTRIM_L3_BACKEND=ladybug  VELANTRIM_L3_PATH=./data/velantrim_l3.lbug
# (LadybugDB persists to disk across restarts; the mock backend is in-memory.)
# ladybug>=0.17.0

# Optional: encryption at rest (GDPR Art. 32). Enables the Fernet/AES backend
# for core/crypto.py when VELANTRIM_ENCRYPTION_KEY is set. Without it, a
# dependency-free HMAC-SHA256 backend is used instead.
# cryptography>=42.0.0

# Future Sprint 2+ dependencies (commented — do not install yet):
# neo4j>=5.15.0
# redis>=5.0.0
# apscheduler>=3.10.0
# prometheus-client>=0.19.0
