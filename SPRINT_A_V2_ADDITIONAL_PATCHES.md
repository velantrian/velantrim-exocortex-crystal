# Velantrim Exo-Cortex Crystal — Historical Sprint-A Patch Pointer

> **Status:** superseded historical design material; not active runtime code, not a current backlog, and not an authorization surface.

The former Sprint-A v2+ document contained large implementation sketches for an older architecture phase. Keeping those snippets in an active root document created unnecessary semantic noise for humans, code search and AI retrieval.

The original content remains preserved in Git history.

For current state use:

- `docs/STATUS.md`;
- `docs/IMPLEMENTATION_STATUS.md`;
- `ROADMAP.md`;
- `docs/SPRINT_A_STATUS.md` when historical Sprint-A interpretation is specifically needed.

Historical snippets must never be inferred to be shipping modules unless corresponding code exists in the current runtime and is covered by the current implementation/evidence surfaces.
